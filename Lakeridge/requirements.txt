# Install torch first with the CUDA wheel for your RTX 4070 Ti:
#   .venv\Scripts\python -m pip install torch --index-url https://download.pytorch.org/whl/cu126
# then: pip install -r requirements.txt
#
# SAM 3 landed in transformers v5 (contributed 2025-11-19), so v5+ is required.

--extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.6

transformers>=5.0.0
accelerate>=1.0.0
huggingface_hub>=0.26.0

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

pillow>=10.0.0
numpy>=1.26.0

# --- VLM incident-narrative layer (Qwen2.5-VL-7B, 4-bit, local) ---
bitsandbytes>=0.49.0    # 4-bit quantization; Windows CUDA wheels since ~0.43
qwen-vl-utils>=0.0.14
av>=18.0.0              # pulled in by qwen-vl-utils

# --- native facebookresearch/sam3 package (installed via: pip install -e ./sam3) ---
# These are required by the native repo but not pinned in its pyproject:
einops>=0.8.0
pycocotools>=2.0.11
timm>=1.0.0
# Windows has no official triton wheel; use the community port:
triton-windows>=3.7.0 ; sys_platform == "win32"
# triton>=3.0.0 ; sys_platform != "win32"
