# DeepSeek-OCR (deepseek-ai/DeepSeek-OCR) -- isolated venv.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# The transformers pin is LOAD-BEARING. The trust_remote_code modeling file
# imports `LlamaFlashAttention2`, which was removed after transformers 4.46.
# 4.57.1 (the Unlimited-OCR venv) and 5.x (the GLM-OCR venv) both fail to load
# this model. Hence one venv per engine, never shared.
#
# torch from the cu126 index: CUDA 13 dropped Volta, so a cu13x build has no
# sm_70 kernels for the V100s. Verified sm_70 present, 4 GPUs visible.
#
# flash-attn is the documented attention backend but requires Ampere+, so on
# Volta the loader falls back to eager. run_deepseek.py records which backend was
# actually used in each meta.json.

--index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://pypi.org/simple

torch==2.6.0+cu126
torchvision==0.21.0+cu126
transformers==4.46.3
tokenizers==0.20.3
einops==0.8.2
addict==2.4.0
easydict==1.13
pymupdf==1.28.0
pillow==12.2.0
numpy==2.4.4
safetensors==0.8.0
huggingface_hub==0.36.2

# --- transitive, pinned so a rebuild is byte-comparable ---
Jinja2==3.1.6
MarkupSafe==3.0.3
PyYAML==6.0.3
certifi==2026.7.22
charset-normalizer==3.4.9
filelock==3.29.0
fsspec==2026.4.0
hf-xet==1.6.0
idna==3.18
mpmath==1.3.0
networkx==3.6.1
nvidia-cublas-cu12==12.6.4.1
nvidia-cuda-cupti-cu12==12.6.80
nvidia-cuda-nvrtc-cu12==12.6.77
nvidia-cuda-runtime-cu12==12.6.77
nvidia-cudnn-cu12==9.5.1.17
nvidia-cufft-cu12==11.3.0.4
nvidia-curand-cu12==10.3.7.77
nvidia-cusolver-cu12==11.7.1.2
nvidia-cusparse-cu12==12.5.4.2
nvidia-cusparselt-cu12==0.6.3
nvidia-nccl-cu12==2.21.5
nvidia-nvjitlink-cu12==12.6.85
nvidia-nvtx-cu12==12.6.77
packaging==26.3
regex==2026.7.19
requests==2.34.2
sympy==1.13.1
tqdm==4.70.0
triton==3.2.0
typing_extensions==4.15.0
urllib3==2.7.0
